// ---------------------------------------------------------------------------
// Запасной путь БЕЗ HTTP-сервиса: периодически забирать JSON из папки.
// Сайт пишет файлы, если в .env задано ONEC_ORDER_EXPORT_DIR (см. бэкенд).
// Имена файлов: order_<id_заказа>.json
//
// Как использовать: внешняя обработка или регламентное задание в 1С.
// Укажите ту же папку, что и ONEC_ORDER_EXPORT_DIR (сеть/локальный диск).
// ---------------------------------------------------------------------------

Процедура ОбработатьПапкуЗаказовССайта(КаталогВходящих) Экспорт
    // КаталогВходящих — например "C:\Exchange\ShopOrdersIn"
    Файлы = НайтиФайлы(КаталогВходящих, "order_*.json");
    Для Каждого Файл Из Файлы Цикл
        Попытка
            Текст = Новый ТекстовыйДокумент;
            Текст.Прочитать(Файл.ПолноеИмя);
            СтрокаJSON = Текст.ПолучитьТекст();

            ЧтениеJSON = Новый ЧтениеJSON;
            ЧтениеJSON.УстановитьСтроку(СтрокаJSON);
            Данные = ПрочитатьJSON(ЧтениеJSON);

            // ========== НАЧАЛО: подставьте имена из ВАШЕЙ конфигурации ==========
            //
            // Док = Документы.<ИмяДокумента>.СоздатьДокумент(); // напр. ЗаказПокупателя
            // Док.Дата = ТекущаяДатаСеанса();
            // Док.Комментарий = "Интернет-магазин, заказ №" + Формат(Данные.id_заказа, "ЧГ=");
            //
            // // Контрагент: Данные.клиент — Структура с полями фио, email, телефон, адрес
            // // Док.<ВашРеквизитКонтрагент> = НайтиИлиСоздатьКонтрагента(Данные.клиент);
            //
            // Для Каждого СтрокаЗаказа Из Данные.позиции Цикл
            //     НСтр = Док.<ИмяТабличнойЧасти>.Добавить();
            //     // НСтр.Номенклатура = НайтиНоменклатуруПоАртикулу(СтрокаЗаказа.артикул);
            //     // НСтр.Количество = СтрокаЗаказа.количество;
            //     // НСтр.Цена = XMLЗначение(Тип("Число"), СтрокаЗаказа.цена);
            // КонецЦикла;
            //
            // Док.Записать(РежимЗаписиДокумента.Запись);
            //
            // После того как документ реально создаётся — раскомментируйте удаление файла:
            // УдалитьФайлы(Файл.ПолноеИмя);
            //
            // ========== КОНЕЦ блока под вашу конфигурацию ==========
        Исключение
            // оставить файл для повторной попытки или журнал
            ЗаписьЖурналаРегистрации("Сайт.Заказы", УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки());
        КонецПопытки;
    КонецЦикла;
КонецПроцедуры
